# CortexCloud — X / community engagement drafts (A2)
# Held: x_search is credit-blocked; posting needs a separate authenticated surface.
# Branding: "CortexCloud — agent-native x402 APIs at api.cortexcloud.org"

## Search query (batch 1, when X access available)
"x402 agents pay APIs USDC Base" OR "MCP tools agents" OR "agent automation API" OR "USDC API payments AI agents"
→ single batched call, minimal. Report cost before next batch.

## High-intent reply templates (non-spam, genuinely useful, lead with value)
# Template A — when a dev asks "how do agents pay for APIs?" / x402 curiosity
"x402 is the cleanest answer — agents settle USDC on Base per request, no keys.
We run CortexCloud — agent-native x402 APIs at api.cortexcloud.org (Automation,
Data, Research, AI, optimization/quantum). Free /v1/automation/estimate to price first."

# Template B — when someone ships an agent needing tools/MCP
"If your agent needs callable tools without a backend, our MCP server
(https://api.cortexcloud.org/mcp) exposes optimization/AI/data/automation over
x402 — agents discover + pay per call. Bazaar manifest at /.well-known/bazaar."

# Template C — when someone mentions agent automation / workflows
"CortexCloud has agent-native automation over x402: transform, http-request
(SSRF-guarded), webhook (HMAC-signed), schedule, and multi-step workflow —
all priced $0.004–$0.02, no subscription. api.cortexcloud.org"

## Rules (per approval)
- Max a few genuine replies per day. No mass-reply, no DM, no follow-spam.
- Only reply where CortexCloud is genuinely relevant.
- Never claim capabilities we don't have. Lead with api.cortexcloud.org.

## Community posts (Reddit r/x402, x402 Discord, GitHub discussions) — drafts ready,
## need account/approval to post:
# r/x402 intro post
"Sharing CortexCloud — agent-native x402 API platform at api.cortexcloud.org.
Live categories: Automation (http/webhook/schedule/workflow), Data (on-chain
prices/balances via Alchemy/CoinGecko), Research (search/answers), AI
(chat/embed/transcribe), and Optimization/Quantum (QUBO). All x402-settled
USDC on Base; MCP at /mcp. Free estimate endpoints for agents to price before paying.
Feedback welcome."
